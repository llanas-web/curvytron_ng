{
  "name": "curvytron",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --stream --filter=./packages/* dev"
  },
  "devDependencies": {
    "lerna": "^6.6.2",
    "ts-node": "~8.6.2",
    "typescript": "~3.7.5"
  }
}