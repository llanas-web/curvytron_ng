{
  "name": "curvytron",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines":{
    "node": "16.19.x"
  },
  "scripts": {
    "dev": "pnpm -r --stream --filter=./packages/* dev",
    "start": "pnpm -r --stream --filter=./packages/* start"
  },
  "devDependencies": {
    "lerna": "^6.6.2",
    "ts-node": "~8.6.2",
    "typescript": "~3.7.5"
  }
}